#include <stdio.h>

int main() {

	int x = 5;
	int y = 2;

	
	int z = x + y;
	int a = x - y;
	int b = x * y;
	float c = x / (double) y;
	int d = x % y;
	x++;
	y--;


	printf("%d\n", x);
	printf("%d", y);

	return 0;
}



Augumented assignment operators
//u³atwienia/skróty, które u³¹tawiaj¹ ustawienie wartoœci itp.
//ustawione w bezpoœrednio pod sob¹ dzia³aj¹ tak samo
int x = 10;

//x = x + 2;
//x += 2;

//x = x - 3;
//x -= 3;

//x = x * 4;
//x *= 4;

//x = x / 5;
//x /= 5;

//x = x % 2;
//x %= 2;


Dla wiêkszych stringów

#include <stdio.h>
#include <string.h>

int main() {

	char name[3]; // how many bytes
	
	printf("What's your name? \n");
	//scanf_s("%s", &name);
	fgets(name, 25, stdin);
	name[strlen(name) - 1] = '\0';

	printf("\nYou're %s!!!", name);
	

	return 0;
}

MATH
#include <stdio.h>
#include <math.h>

int main() {

	double A = sqrt(9);
	double B = pow(2, 4);
	int C = round(3.64);
	int D = ceil (3.14);
	int E = floor(3.64);
	double F = fabs (-100);
	double G = log(3);
	double H = sin(45);
	double I = cos (45);
	double J = tan (45);

	printf("\n %lf", J);

	return 0;
}



£ATWE LICZENIE POLA ITP.>
const double PI = 3.14159;
double radius;
double circumference;
double area;

printf("\n enter radius od a circle: ");
scanf_s("%lf", &radius);

circumference = 2 * PI * radius;
area = PI * radius * radius;

printf("\n\t\t\tcircumference: %lf", circumference);
printf("\n\t\t\tarea: %lf", area);


#include <stdio.h>
#include <math.h>

int main() {

	double A;
	double B;
	double C;

	printf(" Enter side A: \n");
	scanf_s("%lf", &A);

	printf(" Enter side B: \n");
	scanf_s("%lf", &B);

	C = sqrt(A * A + B * B);
	printf("\nSide C: %lf", C);
	
	return 0;
}

IF ELSEIF ELSE

	int age;

	printf("\nEnter your age: ");
	scanf_s("%d", &age);

	if (age >= 18) {

		printf("\nYou're are now sined up.");

	}
	else if (age<0)
	{
		printf("\nNot possible");

	}
	else if (age == 0)
	{
		printf("\nKIDDING");

	}
	
	else {
		printf("Hug you");
	}
	
SWITCH NIBY DZIA£A
	char grade;

	printf("\nEnter a letter grade: ");
	scanf_s("%c", &grade);

	switch (grade){
		case 'A':
			printf("Perfect");
			break;
		case 'B':
			printf("NIce");
			break;
		case 'C':
			printf("MEh");
			break;
		case 'D':
			printf("OK");
			break;
		case 'E':
			printf("JUST STOP");
			break;
		case 'F':
			printf("FAILURE");
			break;
		default:
			printf("OH DANG IT");
			break;
	}

COŒ NIE PYKA< ALE DOBRY KOD
#include <stdio.h>
#include <ctype.h>
int main() {
	char unit;
	float temp;
	printf("\nIS the temperatur on F(0) or C(1)?: ");
	scanf_s("%c", &unit);
	unit = toupper(unit);
	if (unit == ‘C’) {
		printf("\nEnter the temp in Celius: ");
		scanf_s("%f", &temp);
		temp = (temp * 9 / 5) + 32;
		printf("The temp in Farenheit is: %lf", temp);

	}
	else if (unit == ‘F’) {

		printf("\nEnter the temp in Farenheit: ");
		scanf_s("%f", &temp);
		temp = (temp - 32) * 5 / 9;
		printf("The temp in Celius is: %lf", temp);
	}
	else {
		printf("Enter valid letter");
	}

	return 0;
}

KALKULATOR(ogólnie operator powinien byæ jako char, ale coœ nie dzia³a)	
int oper;
	double num1;
	double num2;
	double result;
	printf("\nEnter an operator (+(0) -(1) *(2) /(3)): ");
	scanf_s("%d",&oper);
	switch(oper) {
		case 0:
			printf("\nENter num1: ");
			scanf_s("%lf", &num1);
			printf("\nENter num2: ");
			scanf_s("%lf", &num2);
			result = num1 + num2;
			printf("\n result: %lf", result);
			break;
		case 1:
			printf("\nENter num1: ");
			scanf_s("%lf", &num1);
			printf("\nENter num2: ");
			scanf_s("%lf", &num2);
			result = num1 - num2;
			printf("\n result: %lf", result);
			break;
		case 2:
			printf("\nENter num1: ");
			scanf_s("%lf", &num1);
			printf("\nENter num2: ");
			scanf_s("%lf", &num2);
			result = num1 * num2;
			printf("\n result: %lf", result);
			break;
		case 3:
			printf("\nENter num1: ");
			scanf_s("%lf", &num1);
			printf("\nENter num2: ");
			scanf_s("%lf", &num2);
			result = num1 / num2;
			printf("\n result: %lf", result);
			break;
		default:
			printf("\nYOU STUPID");}

OPERATORY LOGICZNE
AND
//logical operators
float temp(20);
bool sunny = true;

if (temp >= 0 && temp <= 30 && sunny) {

	printf("\nThe weather is great\n");
}
else {
	printf("\nOH dang IT\n");
}

OR
float temp(20);
if ( temp >= 30 || temp <= 0) {
printf("\nThe weather is bad\n");
}
else {	printf("\nOH dang IT\n");
}
NOT
bool sunny = false;
if (!sunny) {
	printf("\nThe weather is ok\n");}
else {
	printf("\nOH dang IT\n");}

FUNCTION BASIC
#include <stdio.h>

void newyear() {

	printf("\nHappy new year");
	printf("\nHappy new year");
	printf("\nHappy new year");
	printf("\nHappy new year\n");

}
int main() {

	newyear();
	newyear();
	newyear();
	return 0;
}
CHAR DZIA£A JU¯
void birthday(char name[], int age) {
	printf("\nHappy birthday dear %s", name);
	printf("\nYou are %d years old", age);}
int main() {
	char name[] = "BRO";
	int age = 21;
	birthday(name, age);
	return 0;}

JEŒLI COŒ CHCESZ ZWRÓCIÆ TO TRZEBA DAÆ DANE KTÓRE COŒ ZWRACJ¥
double square(double x) {
	double result = x * x;
	return result;}
int main() {
	double x = square(3.14);
	printf("%lf", x);
	return 0;}
ELEGANCKIE SKRÓTY
int findMax(int x, int y) {
	return(x > y) ? x : y;}
int main() {
	int max = findMax(7, 4);
	printf("%d", max);
	return 0;}
PROTOTYPE FUNCTION
void hello(int age, int date);
int main() {
	int age = 21;
	int date = 29;
	hello(age, date);
	return 0;}
void hello(int age, int date) {
	printf("\nHello, you're %d years old", age);
	printf("\nand today is %d\n", date);}

ZABAWA NA STRINGACH
#include <string.h>
int main() {
	char string1[] = "Bro";
	char string2[] = "Bro";
	//strlwr(string1);
	//strupr(string1);
	//strcat(string1, string2);
	//strncat(string1, string2, 1);
	//strcpy(string1, tring2);
	//strncpy(string1, string2, 1);

	//strset(string1, '?');
	//strnset(string1, 'x', 1);
	//strrev(string1);

	//int result = strlen(string1);
	//int result = strcmp(string1, string2);
	int result = strcmpi(string1, string2);
	//int result = strnicmp(string1, string2,1)
FOR LOOP
int main() {
	for (int i = 10; i >= 0; i-=2 ) {
		printf("\n%d", i);}
	return 0;}

WHILE LOOP
#include <stdio.h>
#include <string.h>
int main() {
	char name[25];
	printf("What's your name: ");
	fgets(name,25, stdin);
	name[strlen(name) - 1] = '\0';
	while (strlen(name)==0) {
		printf("GIve your name");
		printf("What's your name: ");
		fgets(name, 25, stdin);
		name[strlen(name) - 1] = '\0';}
	printf("\nHello %s", name);
	return 0;}
DO WHILE LOOP
#include <stdio.h>
int main() {
	int num = 0;
	int sum = 0;
	 do{	printf("\nEnter the number greater than zero: ");
		scanf_s("\n%d", &num);
		if (num > 0) {
			sum += num;}
	}while (num > 0);
printf("sum: %d", sum);
	return 0;}

SZYBCIUTKA KOLUMNA DZIÊKI PÊTLI W PÊTLI
	int rows;
	int columns;
	char symbol;
	printf("\nEnter # of rows: ");
	scanf_s("%d", &rows);
	printf("\nEnter # of columns: ");
	scanf_s("%d", &columns);
	scanf_s("%c");
	printf("\nEnter a symbol to use: ");
	scanf_s("%c", &symbol);
	for (int i = 1; i <= rows; i++) {
		for (int j = 1;j <= columns; j++) {
			printf("%c", symbol);}
printf("\n");}
CONTINUE I BREAK
int main() {
	for (int i = 1; i <= 20; i++) {	
		if (i == 12) {
			continue;}
		printf("% d\n", i);}
for (int i = 1; i <= 20; i++) {
		
	if (i == 12) {
		break;}
	printf("% d\n", i);}
	return 0;}

ARRAY	
double prices[5];//double prices[5] = {5.00, 10.00, 15.00, 20.00, 25.00};
	prices[0] = 5.00;
	prices[1] = 10.00;
	prices[2] = 15.00;
	prices[3] = 20.00;
	prices[4] = 25.00;


	printf("%.2lf", prices[4]);
LOOP ACROSS ARRAY 
int main() {
double prices[] = {5.00, 10.00, 15.00, 20.00, 25.00};
//printf("%d bytes\n", sizeof(prices));
for (int i = 0; i < sizeof(prices)/ sizeof(prices[0]);i++) {
	printf("%.2lf\n", prices[i]);}
	return 0;}
NICE 2D ARRAY
#include <stdio.h>
int main() {
	/*int num[2][3] = {
					{1,2,3}, 
					{4,5,6}};*/
	int num[2][3];
	int rows = sizeof(num)/sizeof(num[0]);
	int columns = sizeof(num[0]) / sizeof(num[0][0]);
	printf("rows:%d\n", rows);
	printf("columns: %d\n", columns);

	num[0][0] = 1;
	num[0][1] = 2;
	num[0][2] = 3;
	num[1][0] = 4;
	num[1][1] = 5;
	num[1][2] = 6;	
	for (int i = 0; i < rows; i++) {
		for (int j = 0; j < columns;j++) {
			printf("%d", num[i][j]);}
		printf("\n");}
	return 0;}
ARRAY OF STRINGS
#include <stdio.h>
#include <string.h>
int main() {
	char cars[][10] = { "Mustang", "Corvette", "Camaro" };
	strcpy(cars[0], "Tesla");
	for (int i = 0; i < sizeof(cars) / sizeof(cars[0]); i++) {
		printf("%s\n", cars[i]);}
	return 0;}

SWAMP TEH VALUES OF 2 VARIABLES
#include <stdio.h>
#include <string.h>
int main() {
	char x [15] = "water";
	char y [15] = "soda";
	char temp[15];
	strcpy(temp, x);
	strcpy(x, y);
	strcpy(y, temp);
	//temp = x;
	//x = y;
	//y = temp;
	printf("x = %s\n", x);
	printf("y = %s\n", y);
	return 0;}
SORT AN ARRAY
void sort(int array[], int size);
void printArray(int array[], int size);
int main() {

	int array[] = {9,1,8,7,6,4,5,0,3,2};
	int size = sizeof(array) / sizeof(array[0]);

	sort(array, size);
	printArray(array, size);
	return 0;
}
void sort(int array[], int size) {
	for (int i = 0; i < size - 1; i++) {
		for (int j = 0; j < size - 1; j++) {
			if (array[j] > array[j+1]) {
				int temp = array[j];
				array[j] = array[j + 1];
				array[j + 1] = temp;
			}
		}
	}
}
void printArray(int array[], int size) {
	for ( int i= 0; i<size; i++)
	{		printf("%d", array[i]);}}
STRUCTS IN C
#include <stdio.h>
#include <string.h>
struct Player {
	char name[12];
	int score;};
int main() {
	struct Player player1;
	struct Player player2;
	strcpy(player1.name, "Bro");
	player1.score = 4;
	strcpy(player2.name, "Bra");
	player1.score = 5;
	printf("%s\n", player1.name);
	printf("%d\n", player1.score);
	printf("%s\n", player2.name);
	printf("%d\n", player2.score);
	return 0;}
TYPEDEF
#include <stdio.h>
#include <string.h>

//typedef char user[25];

typedef struct 
{
	char name[25];
	char password[12];
	char id;
}User;

int main() {
	User user1 = { "Bro", "password", 123456};
	User user2 = { "Bruh", "password1", 187423 };
	printf("%s\n", user1.name);
	printf("%s\n", user1.password);
	printf("%d\n", user1.id);
	printf("\n");
	printf("%s\n", user2.name);
	printf("%s\n", user2.password);
	printf("%d\n", user2.id);
	return 0;}

AREA OF STRUCTS
#include <stdio.h>
#include <string.h>

typedef struct 
{
	char name[25];
	float gpa;
}Student;
int main() {
	Student student1 = {"Patrick", 3.0};
	Student student2 = { "Mike", 4.0 };
	Student student3 = { "KOWALSKI", 5.0 };
	Student student4 = { "LUMBERJAck", 2.0 };
	Student students[] = { student1, student2, student3, student4 };
	for (int i = 0; i < sizeof(students) / sizeof(students[0]); i++) {
		printf("%-12s\t", students[i].name);
		printf("%.2f\n", students[i].gpa);}
	return 0;}
Wy³uskanie z funkcji
int get_element(int tab[], int index) {
	return tab[index];}
int main() {
	int tab[] = { 10,20,30,40,50 };
	int index = 5;
	printf("element na indeksie %d to: %d\n", index, get_element(tab, index));
	return 0;}

Wyci¹ga z tabel
#include <stdio.h>
void extract_elements(int tab[], int size, int threshold) {
	printf("Element wiekszy niz %d  ", threshold);
	for (int i = 0; i < size; i++) {
		if (tab[i] > threshold) {
			printf("%d  ",tab[i]);}}
	printf("\n");}
int main() {
	int tab[] = {10,15,25, 30, 5};
	int size = sizeof(tab) / sizeof(tab[0]);
	int threshold = 5;
	extract_elements(tab, size, threshold);
	return 0;}
Inny sposób wy³uskani
#include <stdio.h>
#include <malloc.h>

int* extract_elements(int tab[], int size, int* new_size, int threshold) {
    int* result = malloc(size * sizeof(int)); // Rezerwujemy pamiêæ
    *new_size = 0;

    for (int i = 0; i < size; i++) {
        if (tab[i] > threshold) {
            result[*new_size] = tab[i];
            (*new_size)++;
        }
    }
    return result;
}

int main() {
    int tab[] = { 10, 25, 15, 30, 5 };
    int size = sizeof(tab) / sizeof(tab[0]);
    int threshold = 15;
    int new_size = 0;
    int* extracted = extract_elements(tab, size, &new_size, threshold);
    printf("Nowa tablica: ");
    for (int i = 0; i < new_size; i++) {
        printf("%d ", extracted[i]);}
    printf("\n");
    free(extracted); // Pamiêtaj o zwolnieniu pamiêci
    return 0;}
Tablica na #define’ach
#include <stdio.h>
#define A 10
#define B 3
void wyswietl(int tablica[A][B]) {
	for (int i = 0; i < A; i++) {
		for (int j = 0; j < B; j++) {
			printf("%d", tablica[i][j]);
		}printf("\n");}};
int main() {
	int tablica[A][B];
	for (int i = 0; i < A; i++) {
		for (int j = 0; j < B; j++) {
			tablica[i][j] = i * j;}}
	wyswietl(tablica);
	return 0;}

Na ten szeœcian
#include <stdio.h>
#include <math.h>
void policz(double krawedz, double* pole, double* objetosc);
int main() {
	double krawedz, pole, objetosc;
	printf("Podaj dlugosc krawedzi szescianu: ");
	if (scanf_s("%lf", &krawedz)!=1||krawedz<=0) {
		printf("Podano nieprawidlowe dane wejsciowe, krawedz musi byc dodatnia.\n");
		return 1;
	}
	policz(krawedz, &pole, &objetosc);
	printf("Szescian o dlugosci krawedzi %.2lf ma pole powierzchni %.2lf, a objetosc %.2lf.", krawedz, pole, objetosc);
	return 0;
}
void policz(double krawedz, double* pole, double* objetosc) {
	int Liczba= 6;
	*pole = Liczba * pow(krawedz, 2);
	*objetosc = pow(krawedz, 3);
}

ASCII
#include <stdio.h>
void wpiszASCII(int wartosc);
int main() {
	int wartosc;
	printf("Podaj wartosc ASCII (0-255): ");
	if (scanf_s("%d", &wartosc) != 1 || wartosc < 0 || wartosc>255) {
		printf("Podano nieprawidlowa wartosc.\n");
		return 1;
	}
	wpiszASCII(wartosc);
	return 0;
}
void wpiszASCII(int wartosc) {
	if (wartosc < 0 || wartosc>255) {
		printf("Podano nieprawid³owe wartoœci ASCII. Zakres to 0-255.\n");
		return;
	}

	printf("Wartosc %d ma znak ASCII\"%c\"\t8-->%o\t16=>0x%X\n", wartosc, wartosc, wartosc, wartosc);
}

IMPROVED Obwod pole
#include <stdio.h>
int bomba(int bok1, int bok2, int *pole, int *obwod) {
	*pole = bok1 * bok2;
	*obwod = 2 * bok1 + 2 * bok2;
	return 0;
}
int main() {
	int bok1, bok2, pole, obwod;
	int num = 0;
	int sum = 0;
	do {
		printf("\nEnter the bok1: ");
		scanf_s("\n%d", &bok1);
		printf("\nEnter the bok2: ");
		scanf_s("\n%d", &bok2);

		if (bok1 <= 0 || bok2 <= 0) {
			printf("kampai");
		}
		else {
			bomba(bok1, bok2, &pole, &obwod);
		}
	} while (bok1 <= 0 || bok2 <= 0);
	printf("pole: %d", pole);
	printf("obwod: %d", obwod);
	return 0;
}

ENUMS
#include <stdio.h>

enum Day{ndz = 1, pon, wt, sr, czw, pt, sob};

int main() {
	enum Day today = ndz;
	//printf("%d\n", today);

	if (today == ndz || today == sob) {
		printf("\nIt' weekend.");
	}
	else {
		printf("\nGET BACK TO WORK YOU LAZY BOY");
	}
	return 0;
}
RNG IN C
#include <stdio.h>
#include <stdlib.h>
#include <time.h>
int main() {
	srand(time(0));
	int number1 = rand() % 20 + 1;
	int number2 = rand() % 20 + 1;
	int number3 = rand() % 20 + 1;
	printf("\n%d", number1);
	printf("\n%d", number2);
	printf("\n%d", number3);
	return 0;}

GUESS GAME
#include <stdio.h>
#include <stdlib.h>
#include <time.h>
int main() {
	const int MIN = 1;
	const int MAX = 100;
	int guess;
	int guesses = 0;
	int answer;
	//current time as a seed
	srand(time(0));
	//generate random number 
	answer = rand() % MAX + MIN;
	do {
		printf("Enter a guess: ");
		scanf_s("%d", &guess);
		if (guess > answer) {
			printf("TOO HIGH\n");}
		else if(guess < answer){
			printf("TOO LOW\n");}
		else {
			printf("WOW\n");}
		guesses++;
	} while (guess !=answer);
	printf("**********************");
	printf("answer: %d\n", answer);
	printf("guesses: %d\n", guesses);
	printf("**********************");
	return 0;}
DICE IN PYTHON
import random
#? ? ? ? ? ? ?

dice_art = {
    1:("???????????",
       "?         ?",
       "?    ?    ?",
       "?         ?",
       "???????????"),
    2:("???????????",
       "?  ?      ?",
       "?         ?",
       "?      ?  ?",
       "???????????"),
    3:("???????????",
       "?  ?      ?",
       "?    ?    ?",
       "?      ?  ?",
       "???????????"),
    4:("???????????",
       "?  ?   ?  ?",
       "?         ?",
       "?  ?   ?  ?",
       "???????????"),
    5:("???????????",
       "?  ?   ?  ?",
       "?    ?    ?",
       "?  ?   ?  ?",
       "???????????"),
    6:("???????????",
       "?  ?   ?  ?",
       "?  ?   ?  ?",
       "?  ?   ?  ?",
       "???????????")
}

dice =[]
total = 0
num_of_dice = int(input("How many dice?: "))
for die in range(num_of_dice):
    dice.append(random.randint(1,6))

#for die in range(num_of_dice):
#    for line in dice_art.get(dice[die]):
#        print(line)
for line in range (5):
    for die in dice:
        print(dice_art.get(die)[line], end="")
    print()
for die in dice:
    total+=die
print(f"total: {total}")
EASY PEASY
#voice from inside
from sysconfig import get_path_names

print("What? ")
print("Why are you doing this?")
#Variable = string, integer, booleab, float

#string
first_name = "MAAAN"
last_name = "Babe"
your_name = "Catchy"
print(f"Oh, {first_name}")
print(f"Oh, {last_name}")
print(f"Oh, {your_name}")


#Integers
age = 21
quantity = 3
numofstd=10
print(f"You are {age} years old.")
print(f"You have {quantity} dollars.")
print(f"You have {numofstd} students.")

#float
price= 10.99
gpa = 2.3
distance = 2.45
print(f"You need to pay {price} dollars.")
print(f"You have {gpa}.")
print(f"You have {distance} miles.")

#boolean
is_student = False
if is_student:
    print("You are a student.")
else:
    print("You are not a student.")

KOLEKCJE

#list =[] ordered, changable, duplcates ok
#Set = {} unorderd, immutable, add/remove ok, no duplicates
#Touple = () ordered, unchagneable. duplcates ok. faster

fruits = ("apple","orange", "banana", "cherry")
#print(dir(fruits))
#for fruit in fruits:
  #  print(fruit, end=" ")
#print(len(fruits))
#print("pineapple" in fruits)
#fruits[0]="pineapple"
#fruits.append("pineapple")
#fruits.insert(3,"pineapple")
#fruits.sort()
#fruits.reverse()
#print(fruits.index("banana"))
#print(fruits.count("orange"))
#fruits.add("pineapple")
#fruits.remove("orange")
#fruits.pop()
#fruits.discard("banana")


#for fruit in fruits:
   # print(fruit, end=" ")
print(fruits)

QUIZ GAME
questions = ("What is Bartek?",
             "What do you mean?",
             "Why are you keep asking me?",
             "What is going on?")
options=(("a: idk", "b: idk", "c: ik", "d: idk"),
         ("a: ik", "b: idk", "c: ik", "d: idk"),
         ("a: ik", "b: ik", "c: ik", "d: idk"),
         ("a: ik", "b: ik", "c: ik", "d: ik"))

answers = ("c","a","b","d")
guesses = []
score = 0
question_num = 0
for question in questions:
    print("XXXXXXXXX")
    print("Question:", question)
    for option in options[question_num]:
        print(option)

    guess = input("Enter A,B,C,D: ").lower()
    guesses.append(guess)
    if guess == answers[question_num]:
        score+=1
        print("Correct!")
    else:
        print("Incorrect!")
        print(f"{answers[question_num]} is correct answer")
    question_num += 1

print("XXXXXXXXX")
print("Results")
print("XXXXXXXXX")
print("answers: ", end=" ")
for answer in answers:
    print(answer, end=" ")
print()
for guess in guesses:
    print(guess, end=" ")
print()
score=score/len(questions)*100
print(score)

DICTIONARIES
capitals ={"USA":"Washington D.C",
           "India":"New Dehli",
           "China": "Beijing",
           "Russia": "Moscow"}
#print(dir(capitals))
#print(help(capitals))
#print(capitals.get("India"))

#if capitals.get("India"):
#    print("That capital exist")
#else:
#    print("That capital doesn't exist")
#capitals.update({"Germany":"Berlin"})
#capitals.update({"Germany":"Doner"})
#capitals.pop("China")
#capitals.popitem()
#capitals.clear()

#keys = capitals.keys()
#for key in capitals.keys():
#    print(key)

#values = capitals.values()
#for value in capitals.values():
#    print(value)

#items = capitals.items()
for key, values in capitals.items():
    print(f"{key}: {values}")

list comperhension
passing_grades=[expression for value in iterable if condition]

MATCH
def day_of_week(day):
    match day:
        case 1:
            return 'Sunday'
        case 2:
            return 'Monday'
        case 3:
            return 'Tuesdey'
        case 4:
            return 'Wednsdey'
        case 5:
            return 'Thursday'
        case 6:
            return 'Friday'
        case 7:
            return 'Saturday'
        case _:
            return 'Not a valid day'
print(day_of_week(0))

def is_weekend(day):
    match day.lower():
        case 'Saturday'|'Sunday':
            return True
        case 'Monday'|'Tuesdey'|'Wednsdey'|'Thursday'|'Friday':
            return False
        case _:
            return 'Not a valid day'
print(is_weekend("friday"))
GAMBLE PROGRAM
import random
def spin_row():
    symbols=["??", "??", "??", "??", "??", "?"]
    return[random.choice(symbols) for _ in range(3)]
def print_row(row):
    print("*************")
    print(" | ".join(row))
    print("*************")
def get_payout(row,bet):
    if row[0]==row[1]==row[2]:
        if row[0]=='??':
            return 3*bet
        elif row[0]=='??':
            return 4*bet
        elif row[0]=='??':
            return 5*bet
        elif row[0]=='??':
            return 10*bet
        elif row[0]=='??':
            return 2*bet
        elif row[0]=='?':
            return 20*bet
    return 0
def main():
    balance = 100
    print("*******************************")
    print("Welcome to my Python Game Sluts")
    print("Symbols:?? ?? ?? ?? ?? ?")
    print("*******************************")
    while balance > 0:
        print(f"Current balance: ${balance}")

        bet = input("Place your bet amount: ")

        if not bet.isdigit():
            print("Please enter a valid number.")
            continue
        bet=int(bet)
        if bet>balance:
            print("You don't have enough money.")
            continue
        if bet<=0:
            print("Bet must be greater than 0.")
            continue
        balance-=bet
        row = spin_row()
        print("Spinning...\n")
        print_row(row)
        payout = get_payout(row,bet)
        if payout>0:
            print(f"you won ${payout}!!!")
        else:
            print(f"You lose!!!")
        balance+=payout
        play_again = input("Would you like to play again? (y/n): ").lower()
        if play_again != 'y':
            break
    print("********************************************************")
    print(f"Thank you for playing. Your balance is now ${balance}.")
    print("********************************************************")
if __name__ == '__main__':
    main()

ENCRYPTION CODE
import random
import string

chars = " "+ string.punctuation + string.digits+string.ascii_letters
chars = list(chars)
key = chars.copy()

random.shuffle(key)

#print(f"chars:{chars}")
#print(f"key:{key}")

#Encryption
plain_text = input("Enter a message to encrypt: ")
cipher_text = ""
for letter in plain_text:
    index = chars.index(letter)
    cipher_text+=key[index]
print(f"ciphertext:{cipher_text}")

#Decryption
cipher_text = input("Enter a message to Decrypt: ")
plain_text = "" 

for letter in cipher_text:
    index = key.index(letter)
    plain_text +=chars[index]


print(f"ciphertext:{plain_text}")

Programowamnie obiektowe
#object = "bundle" of related attributes (variables) and methods (functions)
#class = (blueprint) used to design structure and layout of an object
class Car:
    def __init__(self, model, year, color, for_sale):
        self.model = model
        self.year = year
        self.color = color
        self.for_sale = for_sale
    def drive(self):
        print(f"Driving {self.color} {self.model}")
    def stop(self):
        print(f"Stopping {self.color} {self.model}")
    def discribe(self):
        print(f"{self.year} {self.color} {self.model}")
car1 = Car("Mustang", 2024, "red", False)
car2 = Car("Corvete", 2025, "yellow", True)
car3 = Car("Charger", 2023, "white", True)

car1.discribe()
car2.discribe()
car3.discribe()

print(car2.model, end=" ")
print(car2.year, end=" ")
print(car2.color, end= " ")
print(car2.for_sale)
Inheritance
#multiple inheritance = inherit from more than one parent class
# C(A,B)
#multilevel inheritance = inherit from parent which inherits
#       from another parent C(B)<-B(A) <-A
SUUUPER
#super() = Fun ised in child class to call methods from parent sperclass
#   Allows to extend the functionality of the inherited methods.
Polymorh
# Polymorh -> many faces (Poly = many, morph = form)
#Two ways to achive it:
#   Inheritance = An object could be treated of the same type as a parent class
#   "Duck typing" = Object must have necessary attributes/methods, „If it looks like a duck and quacks like a duck, it must be a duck.”

Static and instance
#Static method = belongs to the class rather than any object of that class
#       (instance) to utility functions.
# Instance method = Best for operations on instances of the class (object).
# Static method = Best of utility functions that do need access to class data.
CLASSMETHOD
#class method = Allow operations related to the class itself
#               Take(cls) as the first parameter, whoch represents teh class itself
MAGIC METHODS

#magic method = Dunder Methods
class Book:
    def __init__(self,title,author,num_pages):
        self.title = title
        self.author =author
        self.num_pages = num_pages
    def __str__(self):
        return f"{self.title} by {self.author}"
    def __eq__(self,other):
        return self.title == other.title and self.author == other.author
    def __lt__(self,other):
        return self.num_pages < other.num_pages
    def __gt__(self,other):
        return self.num_pages > other.num_pages
    def __add__(self,other):
        return self.num_pages + other.num_pages
    def __contains__(self,keyword):
        return keyword in self.title or keyword in self.author
    def __getitem__(self,key):
        if key == "title":
            return self.title
        elif key == "author":
            return self.author
        elif key == "num_pages":
            return self.num_pages
        else:
            return f"Key{key} was not valid."
book1 = Book("The Hobbit", "J.R.R. Tolkien", 310)
book2 = Book("The Harry Potter and the Philosopher's Stone", "J.K. Rowling", 223)
book3 = Book("The Lion, the Witch and teh Wardrobe", "C.S. Lewis", 172)
#print(book1==book2)
#print(book3>book2)
#print(book3+book2)
#print("Lewis" in book2)
print(book1[''])
PROPERTY DECORATOR
class Rectangle:
    def __init__(self, width, height):
        self._width = width
        self._height = height
    @property
    def width(self):
        return f"{self._width:.1f} cm"
    @property
    def height(self):
        return f"{self._height:.1f} cm"
    @width.setter
    def width(self,new_width):
        if new_width > 0:
            self._width = new_width
        else:
            raise ValueError("New width must be positive")

    @height.setter
    def height(self, new_height):
        if new_height > 0:
            self._height = new_height
        else:
            raise ("New width must be positive")
    @width.deleter
    def width(self):
        del self._width
        print("Width has been deleted")
    @height.deleter
    def height(self):
        del self._height
        print("Height has been deleted")

DECORATORS
#extend function in additions
def add_sprinkles(func):
    def wrapper(*args, **kwargs):
        print("You add sprinkles")
        func(*args, **kwargs)
    return wrapper

def add_fudge(func):
    def wrapper(*args, **kwargs):
        print("You add fudge")
        func(*args, **kwargs)
    return wrapper

@add_sprinkles
@add_fudge
def get_icecream(flavour):
    print(f"Here is your {flavour} icecream")

get_icecream("vanilla")
EXCEPTIONS
#exception = interrupts thsht flow the program
#           (ZeroDivisionError, TypeError, ValueError)
#           1.try 2.except 3.finallytry:
    num = int(input("Enter a number: "))
    print(1/num)
except ZeroDivisionError:
    print("You can't divide by zero DUMMY")
except ValueError:
    print("Eneter only numbers DUMMY")
except Exception:
    print("Something went wrong")
finally:
    print("Do some clean up here")


Plik TXT
import os

file_path = "C:\\Users\\sgtJj\\OneDrive\\Pulpit\\am.txt"

if os.path.exists(file_path):
    print(f"The location '{file_path}' exists!")

    if os.path.isfile(file_path):
        print("That is a file!")
    elif os.patch.isdir(file_path):
        print("T")
else:
    print("That location doesn't exist!")
OR
import os

employees = ["Eugene", "Patrick", "John", "White"]

file_path = "C:\\Users\\sgtJj\\OneDrive\\Pulpit\\output.txt"
try:
    with open(file_path, "w") as file:
        for employee in employees:
            file.write("\n"+employee)
        print(f"Txt file '{file_path}' was created")
except FileExistsError:
    print(f"File '{file_path}' already exists")

JSON
import json
employees = {"name": "Eugene", "age":30, "Job":"cook"}

file_path = "C:\\Users\\sgtJj\\OneDrive\\Pulpit\\output.txt"
try:
    with open(file_path, "w") as file:
        json.dump(employees, file, indent = 4)
        print(f"Json file '{file_path}' was created")
except FileExistsError:
    print(f"File '{file_path}' already exists")

CSV

import csv

employees = [["Name", "Age", "Job"],["Spongebob", 30, "Cook"],["Patrick", 37, "Unemployed"],["Sandy", 20, "Scientist"]]

file_path = "C:\\Users\\sgtJj\\OneDrive\\Pulpit\\output.txt"
try:
    with open(file_path, "w", newline="") as file:
        writer = csv.writer(file)
        for row in employees:
            writer.writerow(row)
        print(f"CSV file '{file_path}' was created")
except FileExistsError:
    print(f"File '{file_path}' already exists")
ALARM

import time
import datetime
import pygame

def set_alarm (alarm_time):
    print(f"Alarm set for {alarm_time}")
    sound_file = "my_music.mp3"
    is_running = True
    while is_running:
        current_time = datetime.datetime.now().strftime("%H:%M:%S")
        print(current_time)
        if current_time == alarm_time:
            print("Wake up")
            pygame.mixer.init()
            pygame.mixer.music.load(sound_file)
            pygame.mixer.music.play()
            while pygame.mixer.music.get_busy():
                time.sleep(1)
            is_running = False
        time.sleep(1)
if __name__ == '__main__':
    alarm_time = input("Enter the alarm time: (HH:MM:SS)")
    set_alarm(alarm_time)

Multithreading (multitasking)
#multithreding = multitasking
import threading
import time
def walk_dog(first, last):
    time.sleep(8)
    print(f"You finish walking {first} {last}")
def take_out_trash():
    time.sleep(2)
    print("You take out trash")
def get_mail():
    time.sleep(4)
    print("You get the mail")
chore1 = threading.Thread(target=walk_dog, args =("Scooby","Doo"))
chore1.start()
chore2 = threading.Thread(target=take_out_trash)
chore2.start()
chore3 = threading.Thread(target=get_mail)
chore3.start()
chore1.join()
chore2.join()
chore3.join()
print("All chores are done")
Request

import requests
import json

def main():

    try:
        many = float(input("How many do you want: "))
        responde = requests.get("https://rest.coincap.io/v3/assets/bitcoin?apiKey=0cca4cfda486b14537e2b41e62c6e40610751f7dc215a14c31df0f0fc9403a04")
        content = float(responde.json()['data']['priceUsd'])
        amount = many * content
        print(f"${amount:,.4f}")

    except requests.RequestException:
        print("missing sth")
    except ValueError:
        print("Numbers only")
    except:
        print("Something went wrong")

if __name__== "__main__":
    main()

import requests

base_ur1 = "https://pokeapi.co/api/v2/"

def get_pokemon_info(name):
    ur1 = f"{base_ur1}/pokemon/{name}"
    response = requests.get(ur1)

    if response.status_code == 200:
        pokemon_data = response.json()
        return pokemon_data
    else:
        print(f"Failed to retrieve data {response.status_code}")

pokemon_name="Typhlosion"
get_pokemon_info(pokemon_name)
pokemon_info =get_pokemon_info(pokemon_name)

if pokemon_info:
    print(f"Name: {pokemon_info['name'].capitalize()}")
    print(f"ID: {pokemon_info['id']}")
    print(f"Height: {pokemon_info['height']}")
    print(f"Weight: {pokemon_info['weight']}")
PyQt5
import sys
from PyQt5.QtWidgets import QApplication, QMainWindow, QLabel
from PyQt5.QtGui import QIcon, QFont
from PyQt5.QtCore import Qt
class MainWindow(QMainWindow):
    def __init__(self):
        super().__init__()
        self.setWindowTitle("My GUI")
        self.setGeometry(700,350,500,200)
        self.setWindowIcon(QIcon("megusta.jpg"))

        label = QLabel("Hi",self)
        label.setFont (QFont("Arial",20))
        label.setGeometry(20,20,500,100)
        label.setStyleSheet("color: red;" "background-color: brown;" 
                            "font-weight:bold"
                            "font-style: italic"
                            "text-decoration: underline"
                            "text-align: center")
        #label.setAlignment(Qt.AlignVCenter)
        # label.setAlignment(Qt.AlignTop)
        # label.setAlignment(Qt.AlignBottom)
        #label.setAlignment(Qt.AlignRight)
        #label.setAlignment(Qt.AlignHCenter)
        #label.setAlignment(Qt.Alignleft)
        label.setAlignment(Qt.AlignCenter)

def main():
    app = QApplication(sys.argv)
    window = MainWindow()
    window.show()
    sys.exit(app.exec_())

if __name__ == '__main__':
    main()

label = QLabel(self)
label.setGeometry(0,0,300,300)

pixmap =QPixmap("megusta.jpg")
label.setPixmap(pixmap)
label.setScaledContents(True)
label.setGeometry((self.width()-label.width())//2,
                  (self.height()-label.height())//2,
                  label.width(),
                  label.height())

self.setStyleSheet("""
QPushButton {
font-size: 40px;
font-family: sans-serif;
padding: 15px 75px;
margin: 25px;
border: 3px solid;
border-radius: 5px;
}
QPushButton#button1 {
background-color: pink;}
QPushButton#button2 {
background-color: purple;}
QPushButton#button3 {
background-color: red;}
""")
STOPWATCH
import sys
from PyQt5.QtWidgets import *
from PyQt5.QtCore import Qt, QTimer, QTime

class Stopwatch(QWidget):
    def __init__(self):
        super().__init__()
        self.time = QTime(0,0,0,0)
        self.time_label = QLabel("00:00:00:00", self)
        self.startbutton = QPushButton("Start", self)
        self.stopbutton = QPushButton("Stop", self)
        self.restartbutton = QPushButton("Restart", self)
        self.timer = QTimer(self)
        self.initUI()

    def initUI(self):
        self.setWindowTitle("Stopwatch")
        self.setGeometry(750,300,300,300)

        vbox = QVBoxLayout()
        vbox.addWidget(self.time_label)


        self.setLayout(vbox)
        self.time_label.setAlignment(Qt.AlignCenter)
        hbox = QHBoxLayout()
        hbox.addWidget(self.startbutton)
        hbox.addWidget(self.stopbutton)
        hbox.addWidget(self.restartbutton)
        vbox.addLayout(hbox)

        self.setStyleSheet("""
            QPushButtom, QLabel{
                padding: 20px;
                font-weight: bold;
            }
            QPushButton {
                font-size: 50px;
            }
            QLabel {
                font-size: 120px;
                color: green;
                background-color: black;
                border-radius: 20px;
            }
        """)

        self.startbutton.clicked.connect(self.start)
        self.stopbutton.clicked.connect(self.stop)
        self.restartbutton.clicked.connect(self.restart)
        self.timer.timeout.connect(self.update_display)


    def start(self):
        self.timer.start(10)
    def stop(self):
        self.timer.stop()
    def restart(self):
        self.timer.stop()
        self.time = QTime(0,0,0,0)
        self.time_label.setText(self.format_time(self.time))
    def format_time(self, time):
        hours = time.hour()
        minutes = time.minute()
        seconds = time.second()
        milliseconds = time.msec()//10
        return f"{hours:02}:{minutes:02}:{seconds:02}.{milliseconds:02}"
    def update_display(self):
        self.time = self.time.addMSecs(10)
        self.time_label.setText(self.format_time(self.time))

if __name__ == '__main__':
    app = QApplication(sys.argv)
    stopwatch = Stopwatch()
    stopwatch.show()
    sys.exit(app.exec_())

WEATHER APP
import sys
import requests
from PyQt5.QtWidgets import *
from PyQt5.QtCore import Qt

class WeatherApp(QWidget):
    def __init__(self):
        super().__init__()
        self.city_label = QLabel("Enter city name: ",self)
        self.city_input = QLineEdit(self)
        self.get_weather_butt = QPushButton("Get Weather",self)
        self.temperature_label = QLabel(self)
        self.emoji_label = QLabel(self)
        self.description_label = QLabel(self)
        self.setGeometry(750, 300, 250, 150)
        self.initUI()

    def initUI(self):
        self.setWindowTitle("Weather App")

        vbox = QVBoxLayout()
        vbox.addWidget(self.city_label)
        vbox.addWidget(self.city_input)
        vbox.addWidget(self.get_weather_butt)
        vbox.addWidget(self.temperature_label)
        vbox.addWidget(self.emoji_label)
        vbox.addWidget(self.description_label)

        self.setLayout(vbox)
        self.city_label.setAlignment(Qt.AlignCenter)
        self.city_input.setAlignment(Qt.AlignCenter)
        self.temperature_label.setAlignment(Qt.AlignCenter)
        self.emoji_label.setAlignment(Qt.AlignCenter)
        self.description_label.setAlignment(Qt.AlignCenter)
        self.city_label.setObjectName('city_label')
        self.city_input.setObjectName('city_input')
        self.get_weather_butt.setObjectName('get_weather_butt')
        self.temperature_label.setObjectName('temperature_label')
        self.emoji_label.setObjectName('emoji_label')
        self.description_label.setObjectName('description_label')

        self.setStyleSheet("""
            QLabel{
                font-family: calibri;
            }
             QLabel#city_label{
                font-size: 40px;
                font-style: italic;
            }
            QLineEdit#city_input{
                font-size: 40px;
            }
            QPushButton#get_weather_butt{
            font-size: 30px;
            font-weight: bold;
            }
            QLabel#temperature_label{
                font-size: 75px;
            }
            QLabel#emoji_label{
                font-size: 100px;
            }
            QLabel#description_label{
            font-size: 50px;
            }
        """)
        self.get_weather_butt.clicked.connect(self.get_weather)

    def get_weather(self):
        api_key = '559a35f73ac3fc64040450ef1be60600'
        city = self.city_input.text()
        url = f"https://api.openweathermap.org/data/2.5/weather?q={city}&appid={api_key}"
        try:
            response = requests.get(url)
            response.raise_for_status()
            data = response.json()

            if data ["cod"] == 200:
                self.display_weather(data)
        except requests.exceptions.HTTPError as httperror:
            match response.status_code:
                case 400:
                    self.display_err("Bad request\nPlease check your input")
                case 401:
                    self.display_err("Unauthorized\nInvalid API key")
                case 403:
                    self.display_err("Forbidden\nAccess denied")
                case 404:
                    self.display_err("Not found\nCity not found")
                case 500:
                    self.display_err("Internal Server Error\nPlease try again later")
                case 502:
                    self.display_err("Bad gateway\nInvalid response from the server")
                case 503:
                    self.display_err("Service Unavailable\nServr is down")
                case 504:
                    self.display_err("Gateway Timeout\nNo response from the server")
                case _:
                    self.display_err(f"HTTP Erroe\n{httperror}")
        except requests.exceptions.ConnectionError:
            self.display_err("Connection error\n Please check your internet connection")
        except requests.exceptions.Timeout:
            self.display_err("Timeout error\n The requesttimeout")
        except requests.exceptions.ToomyRedirects:
            self.display_err("Too many redirects\n Check the URL")
        except requests.exception.RequestException:
            self.display_err(f"Request Error\n{req_error}")
            
    def display_err(self,message):
        self.temperature_label.setStyleSheet("font-size: 30px;")
        self.temperature_label.setText(message)
        self.emoji_label.clear()
        self.description_label.clear()

    def display_weather(self,data):
        self.temperature_label.setStyleSheet("font-size: 75px;")
        temp_k = data["main"]["temp"]
        temp_C = temp_k - 273.15
        weather_id = data["weather"][0]["id"]
        weather_desription = data["weather"][0]["description"]

        self.temperature_label.setText(f"{temp_C:.1f}°C")
        self.emoji_label.setText(self.get_weather_emoji(weather_id))
        self.description_label.setText(weather_desription)

    @staticmethod
    def get_weather_emoji (weather_id):
        if 200 <= weather_id <= 232:
            return "??"
        elif 300 <= weather_id <= 321:
            return "???"
        elif 500 <= weather_id <= 531:
            return "???"
        elif 600 <= weather_id <= 622:
            return "???"
        elif 701 <= weather_id <= 741:
            return "???"
        elif weather_id == 762:
            return "??"
        elif weather_id == 771:
            return "??"
        elif weather_id == 781:
            return "???"
        elif weather_id == 800:
            return "??"
        elif 801 <= weather_id <= 804:
            return "??"
        else:
            return ""
if __name__ == '__main__':
    app = QApplication(sys.argv)
    weather_app = WeatherApp()
    weather_app.show()
    sys.exit(app.exec_())
LITTLE PROFFESOR
import random

def main():
    score = 0
    i = 0
    z = 0
    score = 0
    level = get_level()
    equa = input("Choose what equasion you want (+ - * /): ")
    for i in range(10):
        if level == "1":
            x,y = generate_integer(level)
            answer = int(generate_equasion(equa, x, y))
            guess = int(input(f"{x} {equa} {y} = "))
            for z in range(2):
                if guess == answer:
                    score += 1
                    break
                else:
                    print("EEE")
                    guess = int(input(f"{x} {equa} {y} = "))
                z += 1
            if z == 2:
                print((f"{x} {equa} {y} = {answer}"))
            i += 1
        elif level == "2":
            x,y = generate_integer(level)
            answer = int(generate_equasion(equa, x, y))
            guess = int(input(f"{x} {equa} {y} = "))
            while z == 3:
                if guess == answer:
                    score += 1
                    break
                else:
                    print("EEE")
                    guess = int(input(f"{x} {equa} {y} = "))
                z+=1
            if z == 2:
                print((f"{x} {equa} {y} = {answer}"))
            i+=1
        elif level == "3":
            x,y = generate_integer(level)
            answer = int(generate_equasion(equa, x, y))
            guess = int(input(f"{x} {equa} {y} = "))
            while z == 3:
                if guess == answer:
                    score += 1
                    break
                else:
                    print("EEE")
                    guess = int(input(f"{x} {equa} {y} = "))
                z += 1
            if z == 2:
                print((f"{x} {equa} {y} = {answer}"))
            i+=1

    print(f"Your score is {score}.")
def get_level():
    while True:
        try:
            level = input("Choose level from 1 to 3: ")
            if int(level) == (1):
                return level
            elif int(level) == (2):
                return level
            elif int(level) == (3):
                return level
            else:
                print("Choose level from 1 to 3: ")
        except ValueError:
            print("It must be an integer!!!")

def generate_integer(level):

    if level == "1":
        x = random.randint(0,9)
        y = random.randint(0,9)
        return x,y
    elif level == "2":
        x = random.randint(0,20)
        y = random.randint(0,20)
        return x, y
    elif level == "3":
        x = random.randint(0,100)
        y = random.randint(0,100)
        return x, y

def generate_equasion(equa,x,y):

    while True:
        if equa == "+":
            answer = x + y
            return int(answer)
        elif equa == "-":
            answer = x - y
            return int(answer)
        elif equa == "*":
            answer = x * y
            return int(answer)
        elif equa == "/":
            answer = x / y
            return int(answer)

if __name__== "__main__":
    main()
TIME CONVERTER
import re
import sys

def main():
    print(convert(input("Hours: ")))

def convert(s):
    if time := re.search(r"^(\d{1,2}):?(\d{2})? (AM|PM) to (\d{1,2}):?(\d{2})? (AM|PM)$", s, re.IGNORECASE):
        #ValueErrors
        if time.group(2):
            if int(time.group(2)) >= 60:
                raise ValueError
        if time.group(5):
            if int(time.group(5)) >= 60:
                raise ValueError
        #time1
        hour1 = int(time.group(1))
        if time.group(3) == "PM":
            hour1 += 12
        elif time.group(3) == "AM" and hour1 == 12:
            hour1 -= 12
        if time.group(2) == None:
            time1 = f"{hour1:02}:00"
        else:
            time1 = f"{hour1:02}:{time.group(2)}"
        # time2
        hour2 = int(time.group(4))
        if time.group(6) == "PM":
            hour2 += 12
        elif time.group(6) == "AM" and hour2 == 12:
            hour2 -= 12
        if time.group(5) == None:
            time2 = f"{hour2:02}:00"
        else:
            time2 = f"{hour2:02}:{time.group(5)}"
        return f"{time1} to {time2}"
    else:
        raise ValueError

if __name__ == "__main__":
    main()

#9:00 AM to 5:00 PM
#9 AM to 5 PM
#9:00 AM to 5 PM
#9 AM to 5:00 PM

